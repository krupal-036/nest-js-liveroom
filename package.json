{
  "name": "nest-js-liveroom",
  "version": "1.0.0",
  "description": "NestJS-Liveroom is a real-time WebSocket application demonstrating a production-oriented approach to dynamic room management. It combines **NestJS, Socket.IO, and React** to create scalable live rooms where clients can dynamically join, communicate, and synchronize real-time state.",
  "main": "index.js",
  "scripts": {
    "start": "npm run start:prod --prefix backend",
    "build": "npm run build:frontend && npm run copy:frontend && npm run build:backend",
    "build:backend": "npm run clean:backend && npm run build --prefix backend",
    "build:frontend": "npm run clean:frontend && npm run build --prefix frontend",
    "clean:all": "node scripts/clean.js",
    "clean:backend": "shx rm -rf backend/dist",
    "clean:frontend": "shx rm -rf frontend/dist",
    "copy:frontend": "shx rm -rf backend/public && shx mkdir -p backend/public && shx cp -r frontend/dist/. backend/public/",
    "dev": "concurrently -p \"[{name}]\" -n \"FRONTEND,BACKEND\" -c cyan,magenta \"npm run dev --prefix frontend\" \"npm run start:dev --prefix backend\"",
    "db:reset": "npm run db:reset --prefix backend",
    "format": "concurrently -p \"[{name}]\" -n \"FRONTEND,BACKEND\" -c cyan,magenta \"npm run format --prefix frontend\" \"npm run format --prefix backend\"",
    "prebuild": "node scripts/getfilenames.js && npm run format",
    "postinstall": "npm install --prefix frontend && npm install --prefix backend",
    "docker:build": "docker compose --env-file backend/.env build",
    "docker:up": "docker compose --env-file backend/.env up -d",
    "docker:down": "docker compose --env-file backend/.env down",
    "docker:logs": "docker compose --env-file backend/.env logs -f app",
    "docker:restart": "docker compose --env-file backend/.env restart app",
    "docker:rebuild": "docker compose --env-file backend/.env up -d --build",
    "docker:pull": "docker compose --env-file backend/.env pull",
    "docker:push": "docker compose --env-file backend/.env push app",
    "docker:clean": "docker compose --env-file backend/.env down --rmi local --volumes"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/krupal-036/nest-js-liveroom.git"
  },
  "keywords": [
    "nest-js-liveroom",
    "real-time-chat-application"
  ],
  "author": "krupal-036",
  "bugs": {
    "url": "https://github.com/krupal-036/nest-js-liveroom/issues"
  },
  "homepage": "https://github.com/krupal-036/nest-js-liveroom#readme",
  "devDependencies": {
    "concurrently": "^10.0.4",
    "shx": "^0.4.0"
  }
}